package mandatoryHomeWork.DSA.week12;

import java.util.Arrays;

public class GettheMinDiffvalue {
	
	/*
	 * 
	 * 
	 * Pseudo code
	 * 1. sort the given input;
	 * 2. And use the max and min function to get the each set difference.
	 */
	
	/*//9,4,1,7
	public int getmindiff(int[] nums, int k) {
		 int max = Integer.MIN_VALUE;
		 int min= Integer.MAX_VALUE;
		 int start=0;
		 
		 Arrays.sort(nums);
		 
		 while(start<nums.length) {
			 if(start<k) {
				 if(max<nums[start] ){
					 max=nums[start];
				 }
				 if(min>nums[start]) {
					 min=nums[start];
				 }
			 }
			 else {
				 
			 }
		 }
	}*/
}
