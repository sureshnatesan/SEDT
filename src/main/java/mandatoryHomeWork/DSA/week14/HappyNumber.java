package mandatoryHomeWork.DSA.week14;

import java.util.ArrayList;
import java.util.List;

public class HappyNumber {
	
	
	/*
	 * 
	 * Pseudo code
	 * 1. create a list to store the sum of the given input.
	 * 2.
	 */
	/* 	
	
	 public boolean isHappy(int n) {
	       // int sum=sumReturn(n);
		 List<Integer> list = new ArrayList<>();
		 list.add(sum);
		 while(sum>1){
			 
		 }

	 }
	
	 
	public Object sumReturn(int n){
		 int sum=0;
		 while(n>0) {
			 sum+=Math.pow(n%10, 2);
			 n=n/10;
		 }
			return sum;
			
		 }
	*/

}
